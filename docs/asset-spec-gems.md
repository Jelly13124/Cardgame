# Gem Asset Spec — Removed System

**Status:** superseded; retained only as a tombstone for historical links.

The run-scoped socketable gem system and its runtime assets were removed on
2026-07-02. Current card growth uses in-run upgrades through
`battle_scene/card_upgrade.gd` and the rest-site upgrade modal.

Do not regenerate gem icons, add `run_system/assets/images/gems/`, or restore
Bleed-era gem effects from old plans. Historical design context remains under
`docs/superpowers/`, but current JSON, validators, and `docs/PRD.md` are
authoritative.
