# Codex Prompt — Shop Scene Asset Generation

Copy everything below the `---` line into your codex session.

---

You are working in the Godot 4.6 project at `C:\Users\Jerry\Desktop\Cardgame` — a roguelite deckbuilder with a strict **Hardcore 128 Pixel Wasteland Art** style. The shop scene gameplay code, scene file, and stock-rolling logic are all done (S4 of the Phase 3 completion plan). The scene is functional with a plain dark background — these assets are visual polish.

## Step 1 — Read these files before doing anything else

1. **`docs/asset-spec-shop.md`** — your authoritative work order. Lists every PNG, exact file path, and theme.
2. **`docs/art-style-reference.md`** — approved Hardcore 128 Pixel Wasteland Art visual reference.
3. **`docs/project-rules.md`** §1–§5 — style anchor, sprite pipeline (`#FF00FF` chroma key → transparent PNG), folder/naming conventions.

Reference for visual scale & weight:
- Existing relic icons: `run_system/assets/images/relics/*.png`
- Map background: `run_system/assets/images/map/wasteland_route_map_pixel_bg.png` (closest analog for a full-screen scene backdrop)
- Existing UI panels: `battle_scene/assets/images/ui/`

## Step 2 — Deliverables (2 PNGs, both optional)

- **1× shop interior background** → `run_system/assets/images/shop/shop_interior_bg.png` (1920×1080, opaque)
- **1× shopkeeper NPC sprite** → `run_system/assets/images/shop/shopkeeper.png` (256×384 native, transparent PNG, single static frame)

Both are visual polish — the shop ships functional without them. Skip either if backlog is full.

## Step 3 — Pipeline rules (non-negotiable)

From `docs/project-rules.md` §2–§5:

1. Hardcore 128 Pixel Wasteland Art prompt anchor must appear in every prompt.
2. NPC sprite uses `#FF00FF` magenta background for chroma-key cleanup. Background scene PNGs are opaque (no chroma key).
3. Save to exact path specified in the asset spec — filename and folder structure matter for runtime loading.
4. `.import` files are auto-generated by Godot on first import. Do not write them manually.

## Step 4 — Don't touch

- `battle_scene/assets/images/*` (combat sprites, other slices' domain)
- `run_system/assets/images/{relics,equipment,map,loot_ui}/*`
- Any `.gd`, `.tscn`, or `.json` file (Claude owns gameplay code & content)
- Any `.import` file

## Step 5 — When you're done

1. Print a delivery summary listing the PNGs written and their sizes.
2. If you skipped either asset, say so explicitly.
3. Do NOT commit. Human will review in-game (open `merchant` map node → shop scene) and commit.

If anything in the spec is ambiguous, stop and ask before making a creative call. Style consistency matters more than throughput.
