# Asset Spec — Content Expansion (Wave 3)

**Audience:** codex (asset generation pipeline)
**Owner of code/JSON:** Claude (already implemented)
**Project:** Cardgame (Godot 4.6, Rick-and-Morty-like Offbeat Adult Sci-Fi Cartoon Wasteland roguelite)
**Created:** 2026-05-26

This document is the contract that tells codex which PNGs to generate for the content shipped 2026-05-25 → 2026-05-26 (Phase 5 wave 1 + wave 2 cards, the multi-boss map redesign, the Warden equipment set, and the new uncommon/rare relics).

**Current style as of 2026-05-31:** this spec inherits `docs/art-style-reference.md` and ADR-0012. New generations must use the Rick-and-Morty-like Offbeat Adult Sci-Fi Cartoon Wasteland prompt anchor from `docs/project-rules.md` section 1. File dimensions below are output sizes only, not pixel-art style constraints.

**Every asset in this spec currently uses a placeholder** — gameplay works but the visuals borrow other items' art. When you replace a placeholder, the game picks up the real art automatically (paths are fixed in the JSON).

> **Generation is tool-agnostic.** This doc fixes only *what* to produce (paths, sizes, style, one neon accent per item) — not *how*. There is currently NO external image service wired in (the previous one was dropped). Use whatever image generation your session supports. If you have no image model, the documented fallback is improving the procedural generator `scripts/gen_wave3_content_assets.py`, which already drew crude geometric placeholders at every target path; you **overwrite** those finals in place. Enemy art will not render until Claude flips the 5 new enemies' `sprite_id` to their own folders post-delivery — that is Claude's follow-up, not Codex's.

---

## 0. Style Preamble (Non-Negotiable)

Every prompt for every asset in this doc must preserve the current art direction from `docs/art-style-reference.md` and `docs/project-rules.md` section 1:

```text
original offbeat adult sci-fi cartoon wasteland game art, Rick-and-Morty-like broad adult sci-fi animation energy without copying named characters or exact show designs, thick dark rubbery outlines, flat bright color blocks, simple cel shading, exaggerated asymmetrical proportions, weird junk-tech silhouette, dusty western leather and brass, dented steel, exposed springs, patched cloth, one or two small glowing neon accents, crisp sprite-ready edges, solid #FF00FF magenta background for cleanup or transparent final PNG, no text, no UI frame, no logo
```

Style notes:
- File dimensions in the tables below are output-size requirements only; they do not imply pixel art.
- Use clean cartoon silhouettes, thick dark outlines, flat bright color blocks, and simple cel shading.
- Keep the wasteland-western junk-tech materials: dusty leather, brass, dented steel, exposed springs, patched cloth, rubber hoses, cracked glass.
- Use one or two small neon accents per item or character; do not flood the asset with glow.
- Keep designs original and do not copy named characters or exact show-specific designs.

---

## 1. Pipeline Reminders

| Asset type | Frame size | Frames | Output path |
|---|---|---|---|
| Card art        | 512×320 native | 1                  | `battle_scene/assets/images/cards/player/{card_id}.png` |
| Equipment icon  | 128×128 native | 1                  | `battle_scene/assets/images/equipment/{item_id}.png` |
| Relic icon      | 128×128 native | 1                  | `run_system/assets/images/relics/{relic_id}.png` |
| Regular enemy   | 128×128 native | 4 attack frames    | `battle_scene/assets/images/enemies/{sprite_id}/attack/{sprite_id}_attack_{0..3}.png` |
| Boss enemy      | 192×192 native | 4 attack frames    | `battle_scene/assets/images/enemies/{sprite_id}/attack/{sprite_id}_attack_{0..3}.png` |

Standing rules (all from `docs/project-rules.md` §2–§5):
1. **Sheet generation uses `#FF00FF` (magenta) background** for chroma-key cleanup. Final per-item output must be transparent PNG.
2. **Item / relic / equipment icons are item-only** — no character holding the item, no UI frame, no background scene, no rarity badges, no text. Just the item, centered, on transparent background.
3. **Card art is composition-only** — no UI frame, no cost badge, no title text. The card-framework script overlays those.
4. **Enemy / boss sprites face LEFT** in source PNGs. (Heroes face right; enemies face left.)
5. **Output paths are exact**; mismatch = placeholder fallback.
6. **`.import` files** are generated by Godot on first import. Do not write them manually.
7. **Intermediate sheets** may stay in `<output_dir>/generated_sheet/` if matched to current delivery — drop a `pipeline-meta.json` + `prompt-used.txt` there for traceability.

---

## 2. Cards (13)

Front-image PNGs, 512×320. Each entry lists the placeholder currently in use, the new theme, and the neon accent.

### 2.1 Common (5)

| ID | Title | Effect summary | Theme | Neon accent | Output |
|---|---|---|---|---|---|
| `siphon`        | Siphon       | dmg + block         | Wasteland surgeon's syringe pulling neon-green fluid from an enemy silhouette; tubing visible | toxic green   | `cards/player/siphon.png` |
| `brace`         | Brace        | 0c block, retain    | Figure in low crouch with a battered scrap shield wedged forward; dust scattering on impact | dusty amber   | `cards/player/brace.png` |
| `double_tap`    | Double Tap   | 3 dmg ×2            | Two pistol muzzle flashes side-by-side, second one still firing; spent shell mid-eject       | electric blue | `cards/player/double_tap.png` |
| `scrap_strike`  | Scrap Strike | dmg + draw          | Rusted wrench mid-swing, with a small data-card / circuit board flying loose behind it       | acid yellow   | `cards/player/scrap_strike.png` |
| `last_breath`   | Last Breath  | 0c block + draw, exhaust | Cracked amulet on a chain, single ember dropping; smoke trailing — feels final           | ember orange  | `cards/player/last_breath.png` |

### 2.2 Uncommon (5)

| ID | Title | Effect summary | Theme | Neon accent | Output |
|---|---|---|---|---|---|
| `last_stand`    | Last Stand   | 12 block + draw    | Figure back-to-rust-wall, sword raised; encroaching silhouettes; defensive heroic pose       | warm amber    | `cards/player/last_stand.png` |
| `acid_splash`   | Acid Splash  | AoE dmg + poison   | Shattered glass flask mid-burst, droplets fanning toward multiple silhouetted targets        | toxic green   | `cards/player/acid_splash.png` |
| `focus`         | Focus        | +INT + draw         | Exposed mechanical brain / third-eye socket glowing; cables radiating; introspective         | electric blue | `cards/player/focus.png` |
| `chain_link`    | Chain Link   | dmg + draw          | A heavy hook chain whipping forward, links blurred mid-arc, ending in a barbed claw          | rusted orange | `cards/player/chain_link.png` |
| `iron_will`     | Iron Will    | +STR +CON, exhaust  | Determined wasteland face, jaw clenched, glowing red eye-band; veins on neck                | blood red     | `cards/player/iron_will.png` |

### 2.3 Rare (3)

| ID | Title | Effect summary | Theme | Neon accent | Output |
|---|---|---|---|---|---|
| `carapace`      | Carapace     | huge block + CON    | Interlocking salvaged plates wrapping a torso; visible bolts; like an exoskeleton             | dusty amber   | `cards/player/carapace.png` |
| `flash_bang`    | Flash Bang   | AoE shock, exhaust  | Military stun grenade mid-detonation, blinding white flash with arc-light tendrils            | electric white| `cards/player/flash_bang.png` |
| `bone_breaker`  | Bone Breaker | heavy dmg + vuln    | Massive scrap sledgehammer mid-swing; bone fragments / sparks scattering on impact            | bloody crimson| `cards/player/bone_breaker.png` |

**Cross-card cohesion:** card art is composition-only (no UI). All should match the existing card visual weight — see `cards/player/strike.png`, `cards/player/junk_bomb.png` as scale/lighting references.

---

## 3. Enemies (5)

Each enemy needs **4 attack frames**, 128×128 native (or 192×192 for bosses). Frames are sequenced for a short attack animation (wind-up → strike → recoil → settle). All face LEFT.

### 3.1 Mid-act Boss / Floor-4 Boss — `rust_titan` (192×192)

- **In-game name:** Rust Titan
- **Sprite folder:** `battle_scene/assets/images/enemies/rust_titan/`
- **Output files:** `attack/rust_titan_attack_{0..3}.png` (4 frames, 192×192 each)
- **Theme:** Towering scrap-iron brute, hunched and broad. Mismatched salvaged plates crudely riveted over rusted core armor. One mechanical arm noticeably bigger than the other (the slam arm). Slag-stained pauldrons. Glowing furnace vent in the chest plate.
- **Neon accent:** molten orange (chest vent + a few seams)
- **Animation:** wide overhead two-hand slam (frame 0 wind-up arms high → 1 mid-slam → 2 impact + dust kick → 3 recovery hunched)

### 3.2 Late-act Boss / Floor-8 Boss — `ash_warden` (192×192)

- **In-game name:** Ash Warden
- **Sprite folder:** `battle_scene/assets/images/enemies/ash_warden/`
- **Output files:** `attack/ash_warden_attack_{0..3}.png` (4 frames, 192×192 each)
- **Theme:** Tall plate-armored figure, soot-blackened plates with ember-glow streaks running through cracks. Faceless helm with a horizontal eye-slit glowing red. Carries a long blackened poleaxe / halberd. Embers visibly drift off the shoulders. Less brute, more sentinel.
- **Neon accent:** ember red-orange (eye slit, crack-streaks, drifting embers)
- **Animation:** halberd thrust + sweep (0 weapon back over shoulder → 1 thrust forward → 2 lateral sweep with ember trail → 3 return to guard)

### 3.3 Mid Enemy — `slag_walker` (128×128)

- **Sprite folder:** `battle_scene/assets/images/enemies/slag_walker/`
- **Output files:** `attack/slag_walker_attack_{0..3}.png` (4 frames, 128×128 each)
- **Theme:** Lumbering construct stitched together from cooled molten slag chunks; uneven silhouette, big right arm fused into a flat mauler. Embers smoldering between cracks. Slag-grey base with orange seams.
- **Neon accent:** dim orange (seam glow)
- **Animation:** heavy single-arm swing (0 arm cocked back → 1 sweeping low → 2 follow-through → 3 settling)

### 3.4 Mid Enemy — `acid_spitter` (128×128)

- **Sprite folder:** `battle_scene/assets/images/enemies/acid_spitter/`
- **Output files:** `attack/acid_spitter_attack_{0..3}.png` (4 frames, 128×128 each)
- **Theme:** Squat low-slung creature with a bloated chemical sac on its back. Wide-set mouth perpetually leaking green fluid; six small limbs. Tubes/valves jutting from the spine connect to the sac.
- **Neon accent:** toxic green (sac glow, mouth drip, projectile trail)
- **Animation:** lean-back wind-up then spit (0 lean back inflating sac → 1 head forward, projectile leaving mouth → 2 acid arc mid-air → 3 settle, sac drained)

### 3.5 Late Enemy — `chrome_hound` (128×128)

- **Sprite folder:** `battle_scene/assets/images/enemies/chrome_hound/`
- **Output files:** `attack/chrome_hound_attack_{0..3}.png` (4 frames, 128×128 each)
- **Theme:** Quadrupedal mechanical hound with polished chrome plating (one of the few clean metal items in the world — clearly maintained by something). Glowing slit-eyes; segmented spine; jaw exposed showing piston-driven teeth.
- **Neon accent:** electric blue (eyes + spinal joint glow)
- **Animation:** lunge bite (0 crouched and coiled → 1 launching forward → 2 bite snapping jaws closed → 3 landing low)

---

## 4. Equipment (5) — Warden Set

The Warden 5-piece set ties to the Ash Warden mid-boss; visuals should read as scavenged from a fallen Warden — same ember-streaked black plate motif, but **without** the boss-tier glow intensity. Cohesive across all 5 pieces (cross-check side-by-side before shipping).

128×128, single frame, transparent background, item-only composition (no character holding it).

| ID | Item Name | Slot | Theme | Neon accent | Output |
|---|---|---|---|---|---|
| `warden_helm`     | Warden Helm     | head      | Soot-blackened steel helm, horizontal eye-slit, faint ember-rim along the brow                  | ember orange | `equipment/warden_helm.png` |
| `warden_vest`     | Warden Vest     | chest     | Ember-pocked black breastplate; cracks running through the plate, faint glow inside the cracks  | ember orange | `equipment/warden_vest.png` |
| `warden_axe`      | Warden Axe      | weapon    | Heavy single-bit axe, blackened steel head, glowing seam where edge meets the haft              | ember orange | `equipment/warden_axe.png` |
| `warden_gloves`   | Warden Gloves   | hands     | Black gauntlets, ember crawling between knuckle plates and along the fingertips                 | ember orange | `equipment/warden_gloves.png` |
| `warden_pendant`  | Warden Pendant  | accessory | A chunk of still-glowing coal in a leather pouch / iron cage on a chain; warm light leaking out | ember orange | `equipment/warden_pendant.png` |

**Set coherence checklist (before delivery):**
- Same soot-black base across all 5 pieces.
- Ember-orange accent is the ONLY accent — no other neons.
- Ember intensity stays subtle on the equipment (the boss itself has the louder glow; gear is the cooled-down version).
- All 5 pieces share the same outline weight and shading granularity.

---

## 5. Relics (4)

128×128, single frame, transparent background, item-only. Drop on top of the dusty workbench look from existing relics (`cracked_battery.png`, `repair_kit.png`).

| ID | Title | Effect summary | Theme | Neon accent | Output |
|---|---|---|---|---|---|
| `bulk_actuator`     | Bulk Actuator       | +10 start-of-combat block       | Heavy hydraulic actuator piston; oily steel cylinder, brass fittings, exposed coil at the top | electric blue | `relics/bulk_actuator.png` |
| `scavenger_lens`    | Scavenger's Lens    | +12 gold per combat win         | Old brass-rimmed magnifying lens on a leather cord; dust on the glass, a tiny gold fleck stuck to the rim | warm gold | `relics/scavenger_lens.png` |
| `inertial_dampener` | Inertial Dampener   | -1 dmg from every enemy hit     | Gyroscopic stabilizer disc — concentric rings on a central pivot, slowly spinning visual implied by motion blur on the outer ring | cyan | `relics/inertial_dampener.png` |
| `war_horn`          | War Horn            | +2 dmg on every attack          | Curled brass / bone war horn with a leather strap; weathered, with a small red rag tied to the strap | crimson red | `relics/war_horn.png` |

---

## 6. Delivery Order (recommended)

Highest-visibility-first so each batch is independently shippable:

1. **Cards (13)** — players see these every battle. Front art change is instantly visible in the deck viewer, loot draft, and battle hand. Start here.
2. **Enemies (5)** — players see new enemies at floors 4 / 8 (bosses) and mid/late combats. Tied to the multi-act map redesign — without these, players see "old enemy with new HP".
3. **Equipment Warden set (5)** — boss-themed reward set; players see them once Warden boss starts dropping gear.
4. **Relics (4)** — smallest visual footprint; usable as last batch.

Bundle a `pipeline-meta.json` per asset capturing prompt + neon accent + frame size + delivery timestamp, dropped in the generated_sheet/ folder next to the source frames.

---

## 7. What NOT to do

- Do not edit any `.gd`, `.tscn`, or JSON files. Code/data are Claude's domain (per ADR-0005).
- Do not invent new effects or rename items — match the JSON `id` exactly.
- Do not bake UI frames, rarity badges, cost circles, or stat numbers into the art.
- Do not change frame sizes from the table in section 1.
- Do not regenerate already-shipped art unless the per-asset entry here explicitly asks for it.
- Do not commit `.import` files (Godot writes them on import).
- Do not commit API keys or secrets. If your generation method calls an external service, read its credentials from an environment variable in your shell session — never bake them into a committed file.

---

## 8. Acceptance

A delivery is accepted when, for each item in sections 2–5:
- The exact output path exists with the correct dimensions
- The PNG has a transparent background (no `#FF00FF` leftover, no opaque rectangle)
- The silhouette reads at the in-game scale (cards at 160×220 hand, enemies at full size, icons at 48×48 panel slot)
- The neon accent is the only neon — no rainbow palette accidents
- All set-grouped items pass the cohesion checklist in their section

Claude will pull and visually verify each batch.
